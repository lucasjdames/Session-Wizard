{
  "name": "session-wizard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx live-server --quiet --watch=./ ./",
    "build": "echo \"No build step yet\"",
    "bump-and-build": "node ./tools/version-bump/bump-and-build.js"
  },
  "dependencies": {
    "jsdom": "^22.1.0"
  }
}
