{
  "name": "session-wizard",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx live-server --quiet --watch=./ ./",
    "build": "echo \"No build step yet\"",
    "clean": "node ./tools/maintenance/clean.js",
    "bump-and-build": "node ./tools/version-bump/bump-and-build.cjs"
  }
}
