// Service worker removed from active project and archived in _archived_cleanout/pwa/
// This stub prevents accidental registration if some build still references the file.
// To restore PWA support: move the archived file back to project root and re-add references in index.html and the build configs.

